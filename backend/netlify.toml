[build]
  command = "npm run build"
  functions = "functions"
  publish = "dist"

[dev]
  functions = "functions"
  publish = "dist"
  port = 8888

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[functions]
  node_bundler = "esbuild"
